{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "if [ \"$SERVICE_TYPE\" = \"frontend\" ]; then yarn preview --port $PORT --host 0.0.0.0; else cd backend && uvicorn server:app --host 0.0.0.0 --port $PORT; fi",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}